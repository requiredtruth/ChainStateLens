# Deliberate next steps

- Add opt-in CSV projection while retaining the complete JSON evidence record.
- Add EIP-1898 hash-pinned collection for nodes that explicitly support it.
- Add configurable bytecode-size safety limits.
